from snakemake.utils import update_config

# This is where the user can define their own 
# custom parameter values - any values that 
# don't make sense to add to the github repo.

config_default = {
    'are_you_a_cow' : False
}

update_config(config_default, config)
config = config_default

